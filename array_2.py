# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

A = 3
B = 1
C = [2, 2, 2]

max=0
# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

A = 3
B = 1
C = [2, 2, 2]

max=0
# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

A = 3
B = 1
C = [2, 2, 2]

max=0
# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

A = 3
B = 1
C = [2, 2, 2]

max=0
# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

A = 3
B = 1
C = [2, 2, 2]

max=0
# given an array A and an non negative number B wre need to find the number of subarray a with thr sum less than b
# a=[2,5,6]
# b=10
# count=0
# for i in range:
#     for j in range(0,N):
#         sum+=A[i]
#         if sum<b:
#             count+1
            
# Given an array of integrs of a given an subarray of and array is said to be good if its fullfill of the area length of a subarray it to be 
# even and sum of theelement of the array must the less then b length of the odd and the of the element is greater thhan b and task is u 
# find an array A and an non negative number B we need to find the number of subarray a with thr sum less than b

# A = [2,5,6]
# B = 10
# count = 0
# for i in range(len(A)):
#     sum= 0
#     for j in range(i, len(A)):
#         sum += A[j]
#         length = j - i + 1

#         if (length % 2 == 0 and sum < B) or (length % 2 == 1 and sum > B):
#             count += 1

# print( count)

# given an array an integer size c of a now u need find a subarray so that the continuous element the sum of continuous array is maximum but 
# not exceed b

# given an aaray len n y task to find maximum possible sum of any non empty contiguou subarray in determine the one that yills the highest sum and 
# return the sum

A = [1, 11, 2, 3, -15]

max_sum = A[0]
curr_sum = A[0]

for i in range(1, len(A)):
    curr_sum = max(A[i], curr_sum + A[i])
    max_sum = max(max_sum, curr_sum)

print(max_sum)
